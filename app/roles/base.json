{
	"name": "base",
	"description": "Base VM configuration",
	"chef_type": "role",
	"json_class": "Chef::Role",
	"run_list": [
		"recipe[init::default]",
		"recipe[cli::install]",
		"recipe[ssh::configure]"
	]
}
