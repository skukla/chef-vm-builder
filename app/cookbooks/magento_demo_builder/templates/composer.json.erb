{
	"name": "skukla/module-custom-demo-shell",
	"version": "1.0.0",
	"description": "N/A",
	"type": "magento2-module",
	"require": {
		"magento/framework": "*",
		"magentoese/module-data-install": "*"
	},
	"license": [
		"GNU General Public License (GPL)"
	],
	"autoload": {
		"files": [
			"registration.php"
		],
		"psr-4": {
			"Skukla\\CustomDemoShell\\": ""
		}
	},
	"extra": {
		"map": [
			<% @media_map.each_with_index do |(map_key, map_value), index| %>
			<% next if map_key == "catalog" %>
			[
				"<%= map_value[:module] %>",
				"../../<%= map_value[:codebase] %>"
			]<% if @media_map.length - 1 != index -%>,<% end -%>
			<% end %>
		]
	}
}